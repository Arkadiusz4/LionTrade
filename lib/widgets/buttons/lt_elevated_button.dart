import 'package:flutter/material.dart';

class LtButton extends StatelessWidget {
  const LtButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
